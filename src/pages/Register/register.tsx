import {RegisterForm} from "../../components/FormAuth/RegisterForm.tsx";

export const Register = () => {
    return (
        <div className="auth">
            <RegisterForm/>
        </div>
    );
};
